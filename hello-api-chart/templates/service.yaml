apiVersion: v1
kind: Service
metadata:
    name: hello-api
    labels:
        app: hello-api
spec:
    ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
    selector:
        app: hello-api
    type: ClusterIP